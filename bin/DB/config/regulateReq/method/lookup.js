const getCLfield = require("../func/getCLfield");

module.exports = (ctxObj, MToptions) => {
    const {lookup} = ctxObj.reqBody;
    const {payload} = ctxObj.Koptions;
    const {CLdoc} = MToptions;
}